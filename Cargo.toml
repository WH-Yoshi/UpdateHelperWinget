[package]
name = "NewPackageLister"
version = "0.1.5"
edition = "2024"

[profile.release]
opt-level = 3
strip = true
lto = true
codegen-units = 1
panic = "abort"

[dependencies]
eframe = "0.32.0"
egui = "0.32.0"
poll-promise = "0.3.0"

[target.'cfg(windows)'.dependencies]
winapi = "0.3.9"
