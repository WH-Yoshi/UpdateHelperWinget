[package]
name = "NewPackageLister"
version = "0.1.4"
edition = "2024"

[profile.release]
opt-level = 3
strip = true
lto = true
codegen-units = 1
panic = "abort"

[dependencies]
eframe = "0.33.0"
egui = "0.33.0"
poll-promise = "0.3.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
open = "5.3.2"

[target.'cfg(windows)'.dependencies]
winapi = "0.3.9"
